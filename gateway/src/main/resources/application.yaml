#logging.level.org.springframework.security: DEBUG
#logging.level.org.springframework.web: DEBUG

server:
  port: 8081

spring:
  application:
    name: gateway
  config:
    import: configserver:${BANK_CONFIG_URL:http://127.0.0.1:8888}

eureka:
  client:
    serviceUrl:
      defaultZone: ${BANK_EUREKA_URL:http://localhost:8761/eureka/}

