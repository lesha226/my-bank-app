spring:
  application:
    name: notification
  cloud:
    config:
      discovery:
        enabled: true
        serviceId: spring-cloud-config-server

server:
  port: 8088

eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_URL:http://localhost:8761/eureka/}
